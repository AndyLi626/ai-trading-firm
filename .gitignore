# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Runtime / logs
runtime_state/trading_log.jsonl
runtime_state/engine_stdout.log
runtime_state/*.pid
*.log

# Secrets (NEVER commit)
secrets/
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.json.secret

# OpenClaw session transcripts
*.jsonl
.openclaw/

# Large repos (referenced, not owned)
# workspace-research/repos/ tracked separately

# Memory (personal, not versioned)
memory/

# OS
.DS_Store
Thumbs.db
.gsd/

# Runtime state files (operational data, not for VCS)
runtime_state/*.md
runtime_state/*.json
workspace-manager/runtime_state/*.json
shared/state/*.jsonl
